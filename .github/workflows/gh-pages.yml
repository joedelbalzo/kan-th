name: GitHub Pages

on:
  push:
    branches:
      - main  # Change to your default branch if it's different

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        fetch-depth: 0  # Fetch all history for all branches and tags

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'  # Use the latest Node.js version

    - name: Install dependencies
      run: npm install

    - name: Build project
      run: npm run build  # Adjust if your build command is different

    - name: Verify build output
      run: ls -la ./build  # List contents of the build directory to verify correct files

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./build  # Ensure this matches the build output directory
