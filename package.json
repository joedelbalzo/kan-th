{
  "name": "Kaas",
  "version": "1.0.0",
  "description": "root folder for theo",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "concurrently npm install && cd frontend && npm install && npm run build && cd ../backend && npm install && npm run start",
    "build-frontend": "cd frontend && npm run build",
    "start": "concurrently \"npm run start-backend\" \"npm run start-frontend\"",
    "start-frontend": "cd frontend && npm run dev",
    "start-backend": "cd backend && npm run start:dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2",
    "dotenv": "^16.3.1",
    "vite": "^4.5.0"
  }
}
